os: unstable

cache:
  - "%LOCALAPPDATA%\\Yarn"

matrix:
  - nodejs_version: 8

install:
  - ps: Install-Product node $env:nodejs_version
  - set CI=true
  - npm install -g yarn
  - yarn

build: off
version: '{build}'
shallow_clone: true
clone_depth: 1
